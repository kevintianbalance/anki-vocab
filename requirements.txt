# === Core dependencies ===
langdetect>=1.0.9
requests>=2.25.1
urllib3>=1.26.0

# === Optional dependencies ===
# For HTTP requests (alternative to urllib, more robust)
requests>=2.25.1

# For HTML parsing if you want to improve dictionary parsing
beautifulsoup4>=4.9.0
lxml>=4.6.0

# For better JSON handling (though json is built-in)
# ujson>=4.0.0  # faster JSON parsing (optional)

gitpython==3.1.43        # if your script auto-commits to git

